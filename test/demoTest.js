const assert = require('assert');

describe('some demo test', () => {
    it('add two numbers', ()=>{
        assert( 2 + 3 === 5)
    })

    it('multiply two numbers', ()=>{
        assert( 2*3 === 6)
    })
});